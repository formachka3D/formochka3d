# Formochka3D

FastAPI web generator for STL cookie cutters.

## Local run

```bash
pip install -r requirements.txt
uvicorn web.app:app --reload
```

## Render

Build command:

```bash
pip install -r requirements.txt
```

Start command:

```bash
uvicorn web.app:app --host 0.0.0.0 --port $PORT
```
